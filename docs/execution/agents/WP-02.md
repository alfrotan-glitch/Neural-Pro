# WP-02 — Export Independence

**Priority:** **P0**
**Depends on:** WP-00, WP-05 (AssetId)
**Blocks:** WP-03, WP-04
**Concurrent-safe with:** WP-01, WP-09
**Estimated risk:** High

---

## 1. Objective

Export must resolve its media **without** the Preview DOM (ADR-002, INV-002, INV-004).

**Evidence of the defect (D-001):**

```ts
// src/core/engine/render/ExportMediaRegistry.ts
document.querySelectorAll<HTMLVideoElement>('[data-export-media-clip-id]')
```

Those nodes are rendered by `VideoPlayer.tsx:497` from `renderSnapshot.byRole.video` — only the
clips **active at the playhead**. Export scans once, after `setCurrentTime(0)`.

Measured on the default project (`v1_clip [0,18.5)`, `v2_clip [18.5,30.5)`, `v3_clip [30.5,45)`):

```
registry at t=0 → ['v1_clip']
frames with an unresolvable active clip → 795 / 1350  (58.9 %)
clips rendered as a purple placeholder   → v2_clip, v3_clip
```

`audit/repro-export-registry.mts` → exit 1.

## 2. Scope

1. **Add `ExportMediaPool`** (`src/infra/media/ExportMediaPool.ts`) implementing
   [../../contracts/media-assets.md](../../contracts/media-assets.md) §8:
   `prepare(requests, signal)`, `get(clipId)`, `dispose()`.
   * creates **detached** `HTMLVideoElement`/`HTMLImageElement` via `document.createElement`,
     never inserted into the React tree;
   * awaits `loadedmetadata` with a bounded timeout;
   * failure ⇒ typed `MEDIA_PREPARE_FAILED` naming the clip — **never** a placeholder.
2. **Add `resolveMediaForClip(clip): MediaSourceRequest | null`** — the only translation from a
   clip to a media request; uses `AssetId` (from WP-05) or a validated `https:` remote URL.
3. **Delete `ExportMediaRegistry`** (keep a typed re-export for one cycle if any non-export
   tooling uses it: `SHIM-002`, owner WP-02, remove WP-12).
4. **Rewire `CanvasExportRenderer`** and `webcodecsExport.ts` to take the pool instead of the
   registry.
5. **Remove** the dependency on `[data-export-media-clip-id]` from the export path.
6. **Add** the headless export test (Preview unmounted).

## 3. Allowed files

```
src/core/engine/render/ExportMediaRegistry.ts        (delete / SHIM-002)
src/core/engine/render/CanvasExportRenderer.ts
src/features/video-studio/export/services/webcodecsExport.ts
src/features/video-studio/export/services/exportService.ts
src/features/video-studio/export/types/export.ts
src/features/video-studio/playback/services/presentedFrameBarrier.ts
src/features/video-studio/playback/services/mediaSeek.ts  (core/engine/mediaSeek.ts — shared seek primitive)
src/infra/media/**                                   (new)
tests/unit/export/**, tests/integration/export/**    (new)
docs/architecture/export-architecture.md, docs/quality/invariant-register.md, docs/quality/risk-register.md
```

## 4. Prohibited files

```
src/components/player/VideoPlayer.tsx        (owned by WP-03/WP-08 for other reasons; the
                                              data-attribute removal is WP-12 after the SHIM)
src/features/video-studio/playback/services/clipTransformModel.ts   (WP-03)
src/features/video-studio/playback/services/mediaFrameGeometry.ts   (WP-03)
src/store/**                                 (WP-04/WP-05)
audit/**
```

## 5. Prerequisites

* WP-00 CI green; WP-05's `AssetId` type and `AssetRegistry` interface available (the
  **interface** may land in WP-05 before its IndexedDB implementation — coordinate: WP-05
  delivers the interface first, WP-02 consumes it).
* A synthetic fixture project with three sequential clips, committed locally.

## 6. Contracts

[../../contracts/media-assets.md](../../contracts/media-assets.md),
[../../contracts/rendering.md](../../contracts/rendering.md) §7,
[../../architecture/export-architecture.md](../../architecture/export-architecture.md)

## 7. Outputs

| Output | Path |
|---|---|
| `ExportMediaPool` | `src/infra/media/ExportMediaPool.ts` |
| Clip → request resolver | `src/domain/export/resolveMediaForClip.ts` |
| Removed registry | `src/core/engine/render/ExportMediaRegistry.ts` (deleted) |
| Headless export test | `tests/integration/export/headlessExport.test.ts` |

## 8. Tests to add

| # | Test | Asserts |
|---|---|---|
| #1 | Export produces `ceil(duration * fps)` frames, all with resolvable media | frame count + no placeholder path taken |
| #5 | Export succeeds with the Preview component unmounted | **the core INV-002 test** |
| #8 | `resolveMediaForClip` returns a request for **every** clip in a fixture project | INV-004 |
| + | `ExportMediaPool.prepare` fails with `MEDIA_PREPARE_FAILED` naming the clip when an asset is missing | no silent placeholder |
| + | `dispose()` releases every element and URL (balance probe zero) | INV-008 |
| + | Export does not call `document.querySelector` (instrumented) | INV-002 |

## 9. Docs to update

`docs/architecture/export-architecture.md` (§3 pool), `docs/quality/invariant-register.md`
(INV-002, INV-004 → PASS with the command), `docs/quality/risk-register.md` (R-001 → CLOSED),
`docs/quality/release-gates.md` (G-05, G-07).

## 10. Acceptance criteria

1. `npx tsx audit/repro-export-registry.mts` exits **0** and the script was **not modified**.
2. Tests #1, #5, #8 pass; each failed on the pre-change commit (recorded).
3. Export of the default project renders **all three** clips (assert: the placeholder branch is
   never entered).
4. `grep -rn "querySelector" src/core/engine/render/` → no match.
5. The resource balance probe is zero after a completed export **and** after a failed export.
6. No change to preview behaviour (full suite green).

## 11. Commit requirement

`feat(wp-02): add export media pool resolving assets by id`,
`fix(wp-02): remove dom scraping from export media registry`,
`test(wp-02): add headless export and media resolution tests`.

## 12. Merge strategy

Land before WP-03. Rebase onto WP-05's interface commit.

## 13. Conflict risks

| Risk | Mitigation |
|---|---|
| `CanvasExportRenderer.ts` is also owned by WP-03 | WP-02 does media resolution only; WP-03 does geometry. Serialise. |
| WP-05's `AssetRegistry` implementation is late | Consume the **interface**; a null-object implementation is acceptable for this WP's tests |
| Detached `<video>` elements behave differently (seek/paint) | `prepare()` must await a presented frame; add `presentedFrameBarrier` usage; verify with the browser test in WP-06 |

## 14. Rollback

Revert the series. `ExportMediaRegistry` is restored by the revert; no persisted data changes.

## Runtime reconciliation (2026-09-09)

| Item | Change |
|---|---|
| Rationale strengthened | Export independence is no longer only a defect fix (D-001). It is the **mechanism** that makes export verifiable at all: the AI Studio preview frame cannot be driven by an automated browser in the general case, so a **headless, preview-independent** export is the only reproducible proof |
| Scope addition | `ExportMediaPool` must resolve **every** asset independently of the UI, so export can be proven without a user's browser (AS-INV-05) |
| Unchanged | No Cloud Run dependency is introduced; nothing in this WP touches the server |
| New dependency awareness | If WP-13 resolves `P-01` as "downloads blocked in the frame", the **delivery** fallbacks belong to WP-04, not here. This WP delivers the artefact; WP-04 delivers it to the user |

---

## 15. Verification block

```
executable:  npx tsx audit/repro-export-registry.mts → exit ?
             npm test -- export → ? passed / ? failed
             balance probe after export → ?
browser:     not run — WP-06   | headless jsdom export → ?
```
